import Testing
import Foundation
@testable import Tally

@Suite("WeekCalculator")
struct WeekCalculatorTests {
    // TODO: Fill in during step 5 of the build order.
    // Cases worth covering:
    //   • weekStart(for:) returns Monday for any day of the week
    //   • week start across daylight-saving boundaries
    //   • daysOfWeek returns Monday→Sunday in order

    @Test func placeholder() {
        #expect(Bool(true))
    }
}
